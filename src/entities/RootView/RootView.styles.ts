import { StyleSheet } from "react-native"

export const rootViewStyles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
  container: {
    flex: 1,
    paddingVertical: 20,
    paddingHorizontal: 20,
  },
})
